# 代码说明

> 更新日期：2026-03-04

---

## 一、md_parser.py - Markdown解析器

### 类定义
```python
class MDParser:
    def __init__(self, md_path):
        self.md_path = md_path
        self.content = self._read_file()
```

### 核心方法

#### 1. parse() - 主解析方法
```python
def parse(self) -> dict:
    """
    解析markdown文件，提取结构化内容
    
    返回结构：
    {
        'h0': ['课程标题'],
        'h1': ['章节1', '章节2'],
        'h2': [
            {
                'title': '小节名',
                'content': ['文本1', '文本2'],
                'images': ['url1'],
                'video': 'url',
                'audio': 'url',
                'char_count': 50,
                'word_limit_tip': '提示'
            }
        ]
    }
    """
```

#### 2. _parse_content() - 内容解析
```python
def _parse_content(self, content_list) -> dict:
    """
    解析内容列表，提取文本、图片、视频、音频
    
    支持的分隔符：||
    支持的类型：text, image, video, audio, link
    """
```

#### 3. get_word_limit_tip() - 字数提示
```python
def get_word_limit_tip(self, char_count, text_box_count) -> str:
    """返回字数限制和文本框数量提示"""
```

---

## 二、ppt_generator.py - PPT生成器

### 类定义
```python
class PPTGenerator:
    def __init__(self, template_path, md_content):
        self.template_path = template_path
        self.md_content = md_content
        self.presentation = Presentation(template_path)
```

### 拼音表格核心方法

#### 1. _create_pinyin_table() - 创建拼音表格
```python
def _create_pinyin_table(self, slide, left, top, width, height, text, font_size=24):
    """
    创建拼音-汉字对齐表格
    
    实现要点：
    1. 解析文本获取拼音和汉字列表
    2. 计算动态列宽（根据拼音长度）
    3. 自动缩放字号适应宽度
    4. 创建2行xN列表格
    5. 填充拼音和汉字
    6. 禁止换行
    7. 隐藏边框和底色
    """
```

#### 2. _hide_table_style() - 隐藏表格样式
```python
def _hide_table_style(self, table):
    """
    隐藏表格边框和底色
    
    使用lxml操作底层XML：
    - 设置所有边框为 noFill
    - 设置单元格背景为 noFill（透明）
    """
```

#### 3. _parse_pinyin_chars() - 拼音解析
```python
def _parse_pinyin_chars(self, text) -> tuple:
    """
    解析文本，返回拼音和字符列表
    
    使用pypinyin库：
    - Style.TONE：带声调拼音
    - 去掉声调数字，保留拼音字母
    """
```

### 关键算法

#### 动态列宽计算
```python
# 字号转EMU单位
pt_to_emu = lambda pt: int(pt * 914400 / 72)

# 列宽 = 拼音长度 × 字号 × 0.6 + 1个字母宽度
letter_width_emu = pt_to_emu(font_size * 0.5)
col_width = pt_to_emu(font_size * 0.6) * py_len + letter_width_emu
```

#### 自动缩放字号
```python
# 计算所需宽度
total_width = calc_total_width(font_size)

# 如果超出可用宽度，按比例缩小
if total_width > width:
    scale = width / total_width
    actual_font_size = max(int(font_size * scale), 10)  # 最小10号
```

#### 禁止换行
```python
# 单元格禁止换行
cell.text_frame.word_wrap = False
```

---

## 三、XML操作（隐藏边框和底色）

```python
from lxml import etree

# 设置边框为无
for border_name in ['lnL', 'lnR', 'lnT', 'lnB']:
    ln = etree.SubElement(tcPr, '{http://schemas.openxmlformats.org/drawingml/2006/main}' + border_name)
    ln.set('w', '0')
    etree.SubElement(ln, '{http://schemas.openxmlformats.org/drawingml/2006/main}noFill')

# 设置无填充（透明背景）
etree.SubElement(tcPr, '{http://schemas.openxmlformats.org/drawingml/2006/main}noFill')
```

---

## 四、常量与配置

```python
# 最小字号
MIN_FONT_SIZE = 10

# 拼音字体
PINYIN_FONT = 'Arial'

# 汉字字体
CHAR_FONT = 'SimSun'

# 拼音行高占比
PINYIN_HEIGHT_RATIO = 0.45

# 汉字行高占比
CHAR_HEIGHT_RATIO = 0.55
```

---

## 五、错误处理

```python
try:
    # XML操作可能失败
    tcPr.append(ln)
except:
    pass  # 静默失败，不影响PPT生成
```

---

## 六、依赖库

| 库 | 用途 |
|---|------|
| python-pptx | PPT创建和操作 |
| pypinyin | 拼音标注 |
| lxml | XML操作 |
| re | 正则表达式 |
