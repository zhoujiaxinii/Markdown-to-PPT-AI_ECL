# 代码说明

> 更新日期：2026-03-11

---

## 一、md_parser.py - Markdown解析器

### 类定义
```python
class MDParser:
    def __init__(self, md_path):
        self.md_path = md_path
        self.content = self._read_file()
```

### 核心方法

#### 1. parse() - 主解析方法
```python
def parse(self) -> dict:
    """
    解析markdown文件，提取结构化内容
    
    返回结构：
    {
        'h0': ['课程标题'],
        'h1': ['章节1', '章节2'],
        'h2': [
            {
                'title': '小节名',
                'section': '所属章节',
                'content': ['文本1', '文本2'],
                'images': ['url1'],
                'video': 'url',
                'audio': 'url',
                'link': 'url',  # 新增：链接
                'char_count': 50,
                'word_limit_tip': '提示'
            }
        ]
    }
    """
```

#### 2. _parse_content() - 内容解析
```python
def _parse_content(self, content_list) -> dict:
    """
    解析内容列表，提取文本、图片、视频、音频、链接
    
    支持的分隔符：||
    支持的类型：text, image, video, audio, link
    """
```

#### 3. get_word_limit_tip() - 字数提示
```python
def get_word_limit_tip(self, char_count, text_box_count) -> str:
    """返回字数限制和文本框数量提示"""
```

---

## 二、ppt_generator.py - PPT生成器

### 类定义
```python
class PPTGenerator:
    def __init__(self, template_path, md_content):
        self.template_path = template_path
        self.md_content = md_content
        self.presentation = Presentation(template_path)
```

### 核心方法

#### 1. generate() - 主生成方法
```python
def generate(self) -> str:
    """
    生成PPT主流程：
    1. 分析模板，建立索引
    2. 构建页面计划
    3. 填充内容（文本/图片/音视频/链接）
    4. 清除未使用的占位符
    5. 保存输出
    """
```

#### 2. _analyze_templates() - 模板分析
```python
def _analyze_templates(self):
    """
    分析模板，按 (文本框数, 图片数, 媒体类型) 三维索引正文模板
    媒体类型：video > audio > link > None
    """
```

#### 3. _match_content_template() - 模板匹配
```python
def _match_content_template(self, text_count, image_count, media_type=None):
    """
    根据内容匹配最佳模板
    """
```

### 拼音表格核心方法

#### 1. _create_pinyin_table() - 创建拼音表格
```python
def _create_pinyin_table(self, slide, left, top, width, height, text, font_size=24, alignment=None, is_english_only=False):
    """
    创建拼音-汉字对齐表格
    
    实现要点：
    1. 解析文本获取拼音和汉字列表
    2. 计算动态列宽（根据拼音长度）
    3. 自动缩放字号适应宽度
    4. 创建表格（2行xN列 或 1行x1列）
    5. 填充拼音和汉字
    6. 禁止换行
    7. 隐藏边框和底色
    8. 纯英文行合并单元格
    """
```

#### 2. _create_single_pinyin_table() - 单行拼音表格
```python
def _create_single_pinyin_table(self, slide, left, top, width, height, py_list, ch_list, fs=24, alignment=None, is_english_only=False, raw_text=''):
    """
    创建单行拼音表格
    
    参数：
    - is_english_only: 是否为纯英文，True时合并单元格
    - raw_text: 原始文本（用于保留英文空格）
    """
```

#### 3. _hide_borders() - 隐藏表格边框
```python
def _hide_borders(self, tbl):
    """
    隐藏表格边框和底色
    
    使用lxml操作底层XML：
    - 设置所有边框为 noFill
    - 设置单元格背景为 noFill（透明）
    """
```

### 媒体嵌入方法

#### 1. _embed_media_to_placeholder() - 嵌入音视频
```python
def _embed_media_to_placeholder(self, slide, placeholder_name, media_path):
    """
    嵌入音频或视频到占位符位置
    - audio: 嵌入MP3，显示图标，点击可播放
    - video: 嵌入MP4，提取第一帧作为封面
    """
```

#### 2. _embed_link() - 嵌入链接
```python
def _embed_link(self, slide, link_url):
    """
    在幻灯片中添加可点击的链接
    使用 click_action.hyperlink.address 设置超链接
    """
```

### 字号计算方法

#### 1. _adjust_font_size_to_fit() - 自动调整字号
```python
def _adjust_font_size_to_fit(self, s, content, fs):
    """
    自动调整字体大小，确保文本能放入文本框
    最小字号：8pt
    """
```

---

## 三、V17新增功能（2026-03-11）

### 1. 链接支持
- 支持 `{{link}}` 占位符
- 点击后用默认浏览器打开链接

### 2. 英文处理优化
- 英文文本保留空格
- 纯英文行合并单元格（单行单列表格）

### 3. 字号规则优化
- 1-2个文本框+无图片+中文>10字 → 28pt

### 4. 音频优化
- 音频嵌入后显示图标，点击可播放
- 删除文字提示

---

## 四、XML操作（隐藏边框和底色）

```python
from lxml import etree

# 设置边框为无
for border_name in ['lnL', 'lnR', 'lnT', 'lnB']:
    ln = etree.SubElement(tcPr, '{http://schemas.openxmlformats.org/drawingml/2006/main}' + border_name)
    ln.set('w', '0')
    etree.SubElement(ln, '{http://schemas.openxmlformats.org/drawingml/2006/main}noFill')

# 设置无填充（透明背景）
etree.SubElement(tcPr, '{http://schemas.openxmlformats.org/drawingml/2006/main}noFill')
```

---

## 五、关键算法

### 动态列宽计算
```python
# 字号转EMU单位
pt_to_emu = lambda pt: int(pt * 914400 / 72)

# 列宽 = 拼音长度 × 字号 × 0.6 + 1个字母宽度
letter_width_emu = pt_to_emu(font_size * 0.5)
col_width = pt_to_emu(font_size * 0.6) * py_len + letter_width_emu
```

### 自动缩放字号
```python
# 计算所需宽度
total_width = calc_total_width(font_size)

# 如果超出可用宽度，按比例缩小
if total_width > width:
    scale = width / total_width
    actual_font_size = max(int(font_size * scale), 10)  # 最小10号
```

---

## 六、依赖库

| 库 | 用途 |
|---|------|
| python-pptx | PPT创建和操作 |
| pypinyin | 拼音标注 |
| lxml | XML操作 |
| re | 正则表达式 |
| subprocess | 视频帧提取 |
| ffmpeg | 视频封面提取（需安装） |